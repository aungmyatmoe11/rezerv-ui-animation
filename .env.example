# Production deployment URL
# Used for canonical URLs, Open Graph metadata, sitemaps, and JSON-LD structured data.
# Must NOT include trailing slash.
# Override at deploy time if using a custom domain.
NEXT_PUBLIC_SITE_URL=https://rezerv-ui-animation.vercel.app
